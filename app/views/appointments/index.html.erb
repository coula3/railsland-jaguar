<h2>Appointments</h2>

<table>
    <thead>
        <tr>
            <th>Date</th>
            <th>Time</th>
            <th>Vehicle Model</th>
            <th>Model Year</th>
            <th>Status</th>
            <th>Service</th>
            <th>Customer</th>
        <tr>
    </thead>
    
    <% @appointments.each do |app| %>
        <tbody>
            <tr>
                <td><%= app.date.strftime("%b %d, %Y") %></td>
                <td><%= app.time.strftime("%H:%M") %></td>
                <td><%= app.veh_model %></td>
                <td><%= app.model_year %></td>
                <td><%= app.status %></td>
                <td><%= app.service.name %></td>
                <td><%= app.customer.full_name %></td>
            </tr>
        </tbody>
     <% end %>
</table>

<p><%= link_to 'My Workspace', user_workspace_path %></p>