<div class="main_container">
    <div id="my_profile">
        <div class="main_data">
            <% if current_user == @user %>
                <h2>User - <%= link_to full_name(@user), edit_user_path, :title => 'Edit User Profile', class: "h2_link_style" %></h2>
            <% else %>
                <h2>User - <%= link_to full_name(@user), admin_edit_path, :title => 'Edit User Profile', class: "h2_link_style" %></h2>
            <% end %>

            <div id="users_show_p_div">
                <p class="users_show_p">
                    <label class="users_show_label">Email</label>
                    <span class="users_show_data"><%= @user.email %></span>
                </p>
                <p class="users_show_p">
                    <label class="users_show_label">System Role</label>
                    <span class="users_show_data"><%= @user.sys_role %></span>
                </p>
                <p class="users_show_p">
                    <label class="users_show_label">Admin</label>
                    <span class="users_show_data"><%= @user.admin? %></span>
                </p>
                <p class="users_show_p">
                    <label class="users_show_label">Account Status</label>
                    <span class="users_show_data"><%= @user.status.capitalize %></span>
                </p>
                <p class="users_show_p">
                    <label class="users_show_label">Created on</label>
                    <span class="users_show_data"><%= readable_created_at(@user) %></span>
                </p>
            </div>
        </div>

        <nav class="bottom_navs">
            <span class="bottom_navs_span"><%= user_workspace %></span>
            <span class="bottom_navs_span"><%= edit?(@user) %></span>
            <span class="bottom_navs_span"><%= system_users_if_admin?(current_user) %></span>
        </nav>
    </div>
</div>